name: Release
on:
  push:

jobs:
  pull-request:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
      - run: python -c "import os; os.mkdir('a'*261)"
